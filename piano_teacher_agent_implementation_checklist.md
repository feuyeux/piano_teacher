# 钢琴教师 Agent 实现清单

## 1. 目标

把设计稿拆解成可执行的开发任务，形成一条能逐步落地的实现路径。目标不是一次把所有能力做完，而是先跑通最小闭环，再逐步增强。

建议开发顺序：

1. 先打通单次练习闭环
2. 再补长期记录与教师反馈
3. 最后扩展 PDF 转换、复杂对齐和进阶教学能力

---

## 2. 第一阶段：项目骨架与基础约定

### 任务 2.1：定义目录结构

目标：

- 明确曲库、会话记录、分析结果、配置文件的位置

建议目录：

```text
/scores
/scores/<piece-name>/
/sessions
/profiles
/tools
/config
```

验收标准：

- 目录职责明确
- 路径规则固定
- 后续脚本可直接依赖这些路径

### 任务 2.2：定义配置文件

目标：

- 统一管理曲库路径、MIDI 设备名、外部转换工具路径、输出目录

建议配置项：

- `score_library_path`
- `session_output_path`
- `profile_output_path`
- `musicxml_converter_command`
- `midi_input_device`
- `default_tempo_tolerance_ms`
- `default_pitch_tolerance`

验收标准：

- 所有工具入口都从统一配置读取参数
- 不在脚本中硬编码本地路径

### 任务 2.3：定义核心数据模型

目标：

- 固定结构化数据格式，避免后续重复调整

需要先定义的模型：

- `PieceRecord`
- `PracticeSession`
- `PracticeAnalysis`
- `PieceProgressProfile`

验收标准：

- 每个模型都有明确字段定义
- 数据可以序列化为 `json`
- 字段能覆盖第一版闭环

---

## 3. 第二阶段：曲库扫描与谱面索引

### 任务 3.1：实现曲库扫描器

目标：

- 扫描指定目录，识别每首曲子的 `PDF` 和 `MusicXML`

输入：

- 曲库根目录

输出：

- 曲目列表
- 每首曲目的文件清单
- 是否具备可分析谱面

核心规则：

- 同名 `pdf` / `musicxml` 优先归为同一曲目
- 允许一首曲目存在多个版本
- 缺少 `MusicXML` 时标记为“待转换”

验收标准：

- 扫描结果稳定
- 能处理空目录、重复文件、缺失文件
- 输出可写入 `PieceRecord`

### 任务 3.2：实现谱面索引缓存

目标：

- 避免每次启动都重新全量扫描

输出：

- 本地索引文件，如 `piece_index.json`

验收标准：

- 首次生成索引
- 文件变化后可刷新
- 索引和磁盘状态一致

---

## 4. 第三阶段：MusicXML 读取与标准化

### 任务 4.1：实现 MusicXML 元信息解析

目标：

- 从 `MusicXML` 提取标题、作曲者、拍号、调号、速度、总小节数

输出：

- 标准化谱面摘要

验收标准：

- 能正确解析常见钢琴谱
- 解析失败时能给出明确错误

### 任务 4.2：实现小节级结构提取

目标：

- 把谱面转成后续分析可用的数据结构

建议提取内容：

- 小节号
- 音符序列
- 左右手分配
- 时值
- 声部信息
- 休止符
- 节拍位置

验收标准：

- 结果可供 MIDI 对齐使用
- 可定位到小节和拍点

### 任务 4.3：定义内部标准谱面格式

目标：

- 不直接在分析阶段依赖原始 `MusicXML`

建议输出：

- `normalized_score.json`

验收标准：

- 谱面分析和 MIDI 对齐都依赖该标准格式
- 以后更换解析器时，上层逻辑不需要大改

---

## 5. 第四阶段：PDF 转 MusicXML

### 任务 5.1：封装外部转换工具接口

目标：

- 把 PDF 转换流程封装成统一命令入口

输入：

- `pdf_path`

输出：

- `musicxml_path`
- 转换日志
- 转换状态

验收标准：

- 可以捕获成功、失败、部分成功三种状态
- 错误信息可追踪

### 任务 5.2：实现转换后校验

目标：

- 在进入分析前快速判断转换结果是否可用

最低校验项：

- 文件能否被解析
- 是否存在小节
- 是否存在音符
- 是否能区分基本声部结构

验收标准：

- 无效结果不进入后续流程
- 失败曲目会被标记为“需人工检查”

### 任务 5.3：实现 Audiveris + 模型 Review Agent

目标：

- 将 PDF 转 MusicXML、确定性校验、模型 review 串成一个独立入口
- 输出可追踪的 `review_request.json` 和 `score_import_review.json`

已落地文件：

- `app/agents/score_import_review_agent.py`
- `app/tools/audiveris_runner.py`
- `app/tools/musicxml_quality.py`
- `app/prompts/score_import_review_system_prompt.txt`
- `config/score_import_review.example.json`

验收标准：

- 可通过 `--pdf` 调用 Audiveris 转换
- 可通过 `--musicxml` 直接 review 已有 MusicXML
- 无模型配置时稳定降级并保存 review 请求
- 模型输出只做转换质量判断，不直接进入教学反馈

---

## 6. 第五阶段：MIDI 录制闭环

### 任务 6.1：实现录制脚本封装

目标：

- 能从 agent 调用开始录制和停止录制

输入：

- `session_id`
- `piece_id`
- `midi_device`

输出：

- 原始 MIDI log
- 录制状态

验收标准：

- 开始 / 停止可重复调用
- 异常退出时能正确收尾

### 任务 6.2：定义 session 生命周期

目标：

- 每次练习都有完整的会话记录

状态建议：

- `created`
- `recording`
- `stopped`
- `analyzing`
- `completed`
- `failed`

验收标准：

- 任一 session 都能追溯状态变化
- 会话和谱面版本绑定

### 任务 6.3：保存练习元数据

目标：

- 除 MIDI 外，还要记录这次练习的上下文

建议字段：

- 曲目
- 开始时间
- 结束时间
- 用户指令
- 使用谱面版本
- 是否完整练习

验收标准：

- 后续分析和进度追踪可直接复用

---

## 7. 第六阶段：MIDI 解析与谱面对齐

### 任务 7.1：MIDI 日志解析

目标：

- 把 MIDI 文件转成事件流

建议提取：

- note on / note off
- 时间戳
- pitch
- velocity
- channel
- pedal

验收标准：

- 能正确还原音符起止时间
- 支持基本踏板事件保留

### 任务 7.2：实现基础对齐算法

目标：

- 将 MIDI 演奏序列与标准谱面序列对齐

第一版建议：

- 先做单次、顺序、无跳段的对齐
- 后续再支持反复、跳小节、局部重练

输出：

- 对齐后的音符对
- 未匹配音符
- 漏音
- 额外音

验收标准：

- 对齐结果可解释
- 出错时能定位具体小节

### 任务 7.3：实现小节级问题聚合

目标：

- 把音符级问题变成小节级和片段级问题

输出：

- 问题小节列表
- 高频错误片段
- 左手 / 右手问题分布

验收标准：

- 教师 agent 不需要再自己从原始事件推断问题位置

---

## 8. 第七阶段：结构化评分与分析结果

### 任务 8.1：定义评分指标

第一版建议指标：

- `pitch_accuracy`
- `rhythm_accuracy`
- `timing_stability`
- `tempo_stability`
- `missed_notes_count`
- `extra_notes_count`
- `problem_measure_count`

验收标准：

- 每个指标定义清楚
- 指标可稳定复算

### 任务 8.2：生成结构化分析报告

目标：

- 输出统一格式的 `PracticeAnalysis`

报告应包含：

- 总体评分
- 关键问题
- 问题小节
- 速度信息
- 可直接传给教师 agent 的摘要

验收标准：

- 不需要 LLM 就能看懂结果
- 相同输入能得到一致输出

---

## 9. 第八阶段：教师 Agent 反馈层

### 任务 9.1：设计教师反馈输入协议

目标：

- 让教师 agent 只接收必要的结构化数据

输入建议：

- 曲目摘要
- 本次分析结果
- 上一次练习摘要
- 当前阶段目标

验收标准：

- 输入短而稳定
- 不把原始 MIDI 或完整 XML 直接喂给模型

### 任务 9.2：设计反馈模板

目标：

- 固定输出结构，保证可读性和训练价值

建议输出三段：

1. 总评
2. 主要问题
3. 下一步练法

验收标准：

- 反馈能定位到片段
- 建议可执行
- 一次不超过 3 个重点

### 任务 9.3：加入个性化上下文

目标：

- 让反馈体现“持续教学”，而不是一次性评论

可加入信息：

- 最近三次常错小节
- 速度提升情况
- 当前阶段标签

验收标准：

- 能体现进步和重复问题
- 不引入冗长输出

---

## 10. 第九阶段：长期进度追踪

### 任务 10.1：实现曲目画像更新

目标：

- 每次练习后更新 `PieceProgressProfile`

更新内容：

- 累计练习次数
- 最近问题模式
- 最佳稳定速度
- 当前阶段
- 下一目标

验收标准：

- 同一曲目的历史可连续追踪
- 教师 agent 能读取这份画像做上下文判断

### 任务 10.2：实现历史检索

目标：

- 支持按曲目查看练习历史

典型查询：

- 最近一次练习结果
- 最近三次速度变化
- 常错小节变化趋势

验收标准：

- 可供 Hermes 看板展示
- 查询接口清晰

---

## 11. 第十阶段：Hermes 集成

### 任务 11.1：定义用户意图路由

至少支持：

- “开始练习”
- “开始练习某首曲子”
- “结束练习”
- “我弹得怎么样”
- “最近练习进度如何”

验收标准：

- 每个意图都能映射到明确工具调用

### 任务 11.2：定义子 agent 职责边界

建议边界：

- 主控 agent：状态与调度
- 教师 agent：反馈与建议
- 工具层：扫描、转换、录制、分析

验收标准：

- 不让教师 agent 直接做工具计算
- 不让工具层直接生成最终教学语言

### 任务 11.3：定义看板状态展示

建议展示：

- 当前曲目
- 当前 session 状态
- 最新分析结果
- 下一步建议
- 历史练习摘要

验收标准：

- 用户能一眼知道当前处于哪一步

---

## 12. MVP 交付标准

满足以下条件即可认为第一版闭环完成：

1. 能扫描曲库并识别曲目
2. 能读取至少一种可用 `MusicXML`
3. 能开始和结束 MIDI 录制
4. 能生成一次结构化分析结果
5. 能输出教师式反馈
6. 能保存 session 和曲目进度

---

## 13. 推荐实现顺序

建议严格按依赖推进：

1. 配置与数据模型
2. 曲库扫描与索引
3. MusicXML 标准化
4. MIDI 录制
5. MIDI 解析与对齐
6. 结构化评分
7. 教师反馈
8. 长期追踪
9. Hermes 集成
10. PDF 转换增强

原因：

- `PDF -> MusicXML` 是高风险项，不适合作为第一阶段阻塞项
- 教师反馈要建立在稳定分析结果上
- 长期追踪必须依赖前面的 session 和分析格式稳定

---

## 14. 建议的开发任务分组

可以直接按下面方式拆成 issue：

### A 组：基础设施

- 配置文件
- 数据模型
- 目录约定
- 索引缓存

### B 组：谱面处理

- 曲库扫描
- MusicXML 解析
- 标准化谱面格式
- PDF 转换封装

### C 组：录制与分析

- MIDI 录制
- MIDI 解析
- 对齐算法
- 评分报告

### D 组：Agent 层

- 教师反馈协议
- 教师输出模板
- Hermes 意图路由
- 看板展示

### E 组：长期能力

- 进度画像
- 历史检索
- 阶段目标生成

---

## 15. 下一步建议

如果继续往下推进，最有价值的是立刻补三份文档：

1. `数据模型定义`
2. `MIDI 与谱面对齐接口定义`
3. `教师 agent 输入输出协议`

这三份会决定后面代码能不能稳定演进。
